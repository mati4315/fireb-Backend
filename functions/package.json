{
  "name": "functions",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "npm run build && firebase deploy --only functions",
    "logs": "firebase functions:log",
    "backup:avatars": "node scripts/backup-avatars.js"
  },
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "dependencies": {
    "basic-ftp": "^5.0.5",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^5.1.1",
    "sharp": "^0.34.3",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/ws": "^8.18.1",
    "typescript": "^4.9.0"
  },
  "private": true
}
